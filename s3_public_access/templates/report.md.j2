# S3 Public Access Audit Report

**AWS Account:** {{ account_id }}
**Generated:** {{ generated_at }}
**Collector:** {{ collector_name }} v{{ collector_version }}
**Schema Version:** {{ schema_version }}

## Executive Summary

This report presents the results of an automated audit of S3 public access configurations for AWS account {{ account_id }}. The audit evaluates account-level and bucket-level controls against CIS AWS Foundations Benchmark v5.0.0 and maps findings to SOC 2 Trust Services Criteria, NIST SP 800-53 Rev. 5, CIS Controls v8.1, and ISO/IEC 27001:2022.

**Findings summary:**

| Status | Count |
|--------|-------|
| PASS | {{ summary.pass }} |
| FAIL | {{ summary.fail }} |
| ERROR | {{ summary.error }} |
| **Total** | **{{ summary.total }}** |

## Collection Metadata

| Field | Value |
|-------|-------|
| Collected at | {{ collected_at }} |
| Caller ARN | `{{ caller_arn }}` |
| Collector | {{ collector_name }} v{{ collector_version }} |
| Schema version | {{ schema_version }} |

## SOC 2 Trust Services Criteria Coverage

This audit provides evidence for the following SOC 2 criteria. Criterion language is from the 2017 Trust Services Criteria, revised 2022.

{% for criterion_id, criterion in soc2_controls.items() %}
### {{ criterion_id }}: {{ criterion.title }}

**Criterion language:**

> {{ criterion.criterion | trim }}

**Relevance to this audit:**

{{ criterion.relevance_to_collector | trim }}

{% endfor %}

## Control Results

{% for control_id, control in controls.items() %}
### {{ control_id }}: {{ control.control_metadata.title }}

**Severity:** {{ control.control_metadata.severity }}
**Scope:** {{ control.control_metadata.scope }}

**Rationale:**

{{ control.control_metadata.rationale }}

**Framework Mappings:**

| Framework | References |
|-----------|------------|
| {{ framework_labels.soc2 }} | {{ control.control_metadata.maps_to.soc2 | join(', ') }} |
| {{ framework_labels.nist_800_53_r5 }} | {{ control.control_metadata.maps_to.nist_800_53_r5 | join(', ') }} |
| {{ framework_labels.cis_controls_v8 }} | {{ control.control_metadata.maps_to.cis_controls_v8 | join(', ') }} |
| {{ framework_labels.iso_27001_2022 }} | {{ control.control_metadata.maps_to.iso_27001_2022 | join(', ') }} |

**Findings:**

| Resource | Status | Detail |
|----------|--------|--------|
{% for finding in control.findings -%}
| `{{ finding.resource_id }}` | {% if finding.status == 'FAIL' %}**FAIL**{% elif finding.status == 'ERROR' %}**ERROR**{% else %}{{ finding.status }}{% endif %} | {{ finding.detail }} |
{% endfor %}

{% endfor %}

## Scope Declarations

The following items are explicitly out of scope for this audit:

{% for item in out_of_scope %}
- {{ item }}
{% endfor %}

## Required IAM Permissions

This collector operates with read-only permissions. The complete IAM policy document is included in this evidence pack as `collector_iam_policy.json`. The actions used are:

{% for action in required_permissions %}
- `{{ action }}` 
{% endfor %}

---

*This report was generated by an automated evidence collector. Raw API responses are preserved in `raw/` directory for independent verification. Finding-level evidence references in `findings.json` point to specific files under `raw/`.*
